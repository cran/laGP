PKG_CFLAGS = -DRPRINT $(SHLIB_OPENMP_CFLAGS) ## -UNDEBUG
## PKG_CFLAGS = -DRPRINT -fopenmp ## -UNDEBUG 
## PKG_CFLAGS = -DRPRINT -fopenmp -D_GPU ## -UNDEBUG
PKG_LIBS = ${LAPACK_LIBS} ${BLAS_LIBS} ${FLIBS} $(SHLIB_OPENMP_CFLAGS)
## PKG_LIBS = ${LAPACK_LIBS} ${BLAS_LIBS} ${FLIBS} -lgomp
## PKG_LIBS = ${LAPACK_LIBS} ${BLAS_LIBS} ${FLIBS} -lgomp alc_gpu.o -L /software/cuda-5.0-el6-x86_64/lib64 -lcudart
